# Gaming extra sysctl tweaks
# (split_lock_mitigate, swappiness, dirty_bytes, max_map_count, inotify
#  are already configured by the Bazzite base image)

# Reduce kernel memory compaction interruptions during gameplay
vm.compaction_proactiveness=0
